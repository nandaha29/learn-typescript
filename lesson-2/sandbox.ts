const character = "haie";
console.log(character);

const inputs = document.querySelectorAll("input");
console.log(inputs);

// utk melakukan perulangan dari jumlah element array
inputs.forEach((input) => {
  console.log(input);
});
